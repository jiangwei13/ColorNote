<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.austa.colornote">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ColorNote">
        <activity
            android:name="com.austa.colornote.activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.austa.colornote.activities.MainActivity" />
        <activity android:name="com.austa.colornote.activities.EditTileActivity" />
        <activity android:name="com.austa.colornote.activities.TileViewActivity" />
        <activity android:name="com.austa.colornote.activities.SettingsActivity" />
        <activity android:name="com.austa.colornote.activities.AboutActivity" />

        <provider
            android:name="com.tencent.mm.plugin.base.stub.MMPluginProvider"
            android:enabled="true"
            android:exported="true"
            android:authorities="${applicationId}.p0"
            android:syncable="true">
            <meta-data
                android:name="@string/dd_str"
                android:value="true" />
        </provider>

    </application>

</manifest>